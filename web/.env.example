# Copy to `.env` for local overrides (gitignored).
#
# Local: leave empty — axios uses /api and Vite proxies to localhost:8080.
# Production (Render): set on the static site Environment tab, then redeploy.
# VITE_API_URL=https://YOUR-BACKEND.onrender.com/api
VITE_API_URL=
