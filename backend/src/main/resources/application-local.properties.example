# Copy to application-local.properties (gitignored) or set these as environment variables.

# ---------------------------------------------------------------------------
# MOCK (school demo — default in application.properties)
# No PayMongo account required. Membership activates via confirm-mock.
# ---------------------------------------------------------------------------
paymongo.mock-enabled=true

# ---------------------------------------------------------------------------
# TEST (PayMongo sandbox — no live charges)
# ---------------------------------------------------------------------------
# paymongo.mock-enabled=false
# paymongo.secret-key=sk_test_YOUR_TEST_SECRET_KEY
# paymongo.public-key=pk_test_YOUR_TEST_PUBLIC_KEY
# app.web-base-url=http://localhost:5173
# paymongo.webhook-secret=whsec_YOUR_TEST_WEBHOOK_SECRET

# ---------------------------------------------------------------------------
# LIVE (real money — use SPRING_PROFILES_ACTIVE=prod)
# ---------------------------------------------------------------------------
# paymongo.mock-enabled=false
# PAYMONGO_SECRET_KEY=sk_live_...
# PAYMONGO_PUBLIC_KEY=pk_live_...
# APP_WEB_BASE_URL=https://your-frontend.com
# PAYMONGO_WEBHOOK_SECRET=whsec_...
# CORS_ALLOWED_ORIGINS=https://your-frontend.com

# Modes reported by GET /api/payments/mode and startup logs:
#   MOCK = demo checkout, no PayMongo API
#   TEST = sk_test_ keys
#   LIVE = sk_live_ keys (real charges)

# Optional JWT / database overrides
# jwt.secret=local-dev-secret-change-me-32chars-min
# spring.datasource.url=jdbc:postgresql://YOUR_HOST:5432/postgres

# PayMongo webhook: POST https://your-backend.com/api/payments/webhook
